public class Problem7 {
    public static void main(String[] args) {

    }

    private static void ant(String num, int end) {
        char pre = num.charAt(0);
        for(int i = 0; i < num.length(); i++) {
            char now = num.charAt(i);

            if(pre != now) {
                int start = i;
            }
        }
    }
}
